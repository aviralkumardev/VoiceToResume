# Testing

> **Not implemented.** Testing was deliberately scoped out of this implementation pass. If testing is added later, follow the patterns described in the original design documents and use `pytest` with `pytest-asyncio` and `respx` for mocking the OpenAI SDK's HTTP layer.
